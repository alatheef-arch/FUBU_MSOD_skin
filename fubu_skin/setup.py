from setuptools import setup, find_packages

# For the skin repo, the name should be 'fubu_skin'
# For the stringer repo, the name should be 'fubu_stringer'
setup(
    name='fubu_skin',
    version='0.1.0',
    packages=find_packages(),
    description='A dynamic plugin for the FUBU MSOD application.'
)
